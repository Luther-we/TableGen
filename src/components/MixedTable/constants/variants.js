import ArrowTable from '';
import BorderBottomTable from '';
import ExpandedTable from '';

const VARIANTS = {
  arrow: ArrowTable,
  borderBottom: BorderBottomTable,
  expanded: ExpandedTable,
};

export default VARIANTS;
